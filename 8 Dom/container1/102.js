// ----------   1.35.32  se video of harshit vashisht



/*
//------------  nav var  background color change-----   using class name ----getElementsByClassName------

// we can 't use forEach method to iterate through   
// .forEach() method on certain collections like HTMLCollection and NodeList (in some cases). 
  
//  BUT WE CAN CHAGE THE COLLECTINO IN ARRAY USING ARRAY.FOMR("NAV-ITEMES")


// sinple for Zoop 
// for Of loop 
// forEach 

const element = document.getElementsByClassName("nav-item")
console.log(element.length)


for(let i=0;i<element.length;i++){
    console.log(element[i])


    const element_fetch = element[i]
    element_fetch.style.backgroundColor = "#fff"
    element_fetch.style.color = "green"  /// nhi hoga kyuki
}


*/



/*

//------------  nav var  color change-----   using tag name ---getElementsByTagName-------

const anchor_tagName_color = document.getElementsByTagName("a")
console.log(anchor_tagName_color.length)
console.log(anchor_tagName_color)

for(let i=0;i<anchor_tagName_color.length;i++){
    console.log(anchor_tagName_color[i])


    const element_fetch = anchor_tagName_color[i]
    element_fetch.style.backgroundColor = "#fff"
    element_fetch.style.color = "#ff8800"
    element_fetch.style.fontWeight = "BOLD"  
}

*/




/*

//------------  nav var  background color change-----   using QuerySelectorAll name ----------

*/

